# binary: suppress the noisy textual diff (sets -diff -text).
# merge=ours: auto-resolve every merge/rebase conflict by keeping the local
#   version. Needs `merge.ours.driver = true` in git config, set in home/common.nix.
flake.lock binary merge=ours
